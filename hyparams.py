seed = 0                            # random seed

framerate = 4						# frames per sec
cell_size = 5                       # cell size
cell_number = 30                    # no of cells in width
offset = 3                          # cell padding around simulation

number_of_particles = 20            # no of particles in simulation
screen_update_timer = 1500          # time between game updates in millisec

alive_color = (0,0,255)             # alive state color
dead_color = (0,0,0)                # dead state color

num_processes = 8

sim_offset = offset * cell_size

filename = 'P:\\conway_game_life\\board_states.csv'

csv_step = 10
